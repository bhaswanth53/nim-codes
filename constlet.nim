# const vs let
#const will be used to declare constant variables which we can't either change again or mix other variables with them.

const G = 9.81
echo G

#let will be used to declare constant variables which we can't change again but we can mix other variables with it.

var a = 5
let const_a = a + 4

echo const_a